defaults:
  - _self_
  - model: resnet_conformer
  - override hydra/hydra_logging: disabled
  - override hydra/job_logging: disabled
hydra:
  output_subdir: null
  run:
    dir: .
exp_dir:
exp_name:
gpus:
slurm_job_id:
filename:
ckpt_path:
pretrained_model_path:
transfer_frontend:
transfer_encoder:
enable_legacy: false
save_mouth_roi: false
